<% content_for :title, 'New Menu Item' %>

<%= simple_form_for @menu_item, url: admin_menu_items_path(@menu) do |f| %>
  <%= render 'form_fields' %>
  <%= f.submit 'Create Menu Item', class: 'btn' %>
<% end %>
