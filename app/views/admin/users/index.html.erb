<% content_for :title, 'Users' %>

<table class="table table-bordered">
  <tr><th>Email</th><th>Actions</th></tr>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.email %></td>
      <td></td>
    </tr>
  <% end %>
</table>
<%= paginate @users %>
