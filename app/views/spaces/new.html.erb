<% content_for :title, 'New Space' %>

<%= link_to 'Back to Dashboard', host_dashboard_path %>

<%= simple_form_for @space do |f| %>
  <%= render 'form_fields' %>
  <%= f.submit 'Create Space', class: 'btn btn-default' %>
<% end %>
